export const enum ProjectType {
    Game,
    Software,
    Asset,
    Project
}

export const enum ProjectContentType {
    Screenshots,
    About,
    Features,
    Requirements,
    Releases,
}

export const enum UserRoles {
    None,
    Admin
}